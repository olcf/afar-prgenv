# CMake MPI + OpenMP

Purpose: exercise CMake-driven builds that pass flags through `ftn/cc/CC`.

Build:
```
./build.sh
```

Notes:
- Requires `cmake` in `PATH`.
- Uses `ftn` and `cc` as the compilers.
