\begin{tikzpicture}[
  scale=0.9,
  transform shape,
  box/.style={draw, rounded corners, align=center, minimum width=2.1cm, minimum height=0.9cm},
  small/.style={draw, rounded corners, align=center, minimum width=1.9cm, minimum height=0.8cm},
  arrow/.style={-Latex, thick}
]
  \node[box] (user) {User build\\(CMake/Make)};
  \node[box, right=1.0cm of user] (wrappers) {Cray wrappers\\\texttt{ftn/\allowbreak cc/\allowbreak CC}};
  \node[box, right=1.0cm of wrappers] (compiler) {AFAR compiler\\drop};

  \node[small, below=1.0cm of wrappers] (pkg) {pkg-config\\metadata};
  \node[small, below=1.0cm of compiler] (rocm) {ROCm toolkit};

  \node[box, below=1.0cm of user] (modules) {Afar-prgenv\\modules};

  \draw[arrow] (user) -- (wrappers);
  \draw[arrow] (wrappers) -- (compiler);
  \draw[arrow] (wrappers) -- (pkg);
  \draw[arrow] (compiler) -- (rocm);
  \draw[arrow] (modules) -- (wrappers);
  \draw[arrow] (modules) -- (compiler);
  \draw[arrow] (modules) -- (rocm);
\end{tikzpicture}
